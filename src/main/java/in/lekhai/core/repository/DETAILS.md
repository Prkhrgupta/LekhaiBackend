# core/repository — details

No deep-dive notes beyond `MODULE.md` — that file is the full picture here.

Add notes here only for non-obvious internals (custom `@Query` contracts,
projection/return-type rules). Never define domain terms here; the glossary
lives in `CONTEXT.md` (see `AGENT.md` Cross-Cutting Rules).
